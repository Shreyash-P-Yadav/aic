# Insight Copilot

This repository is built by following `docs/CLAUDE-CODE-BUILD-PROMPT.md`.
Read that file in full before doing anything else. The design docs it
refers to are in `docs/`.

Current state lives in `BUILD_PROGRESS.md`. Never mark a phase DONE
without pasting the actual output of its gate into `BUILD_LOG.md`.
Commit after every phase.

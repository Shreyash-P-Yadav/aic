[pytest]
testpaths = tests
pythonpath = backend/src
addopts = -q --strict-markers
asyncio_mode = auto
markers =
    slow: long-running tests (data generation, calibration backtest)
    statistical: statistical acceptance tests
filterwarnings =
    error::DeprecationWarning:insight_copilot.*

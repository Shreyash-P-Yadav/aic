"""L5 — the pathology catalog.

Every capability the engine claims has a defect in the data that exercises it. If the
engine claims to reconcile refresh cadences, the data must *have* different cadences;
if it claims to abstain on stale feeds, a feed must actually go stale. A clean dataset
proves nothing — it makes an untested system look finished.
"""

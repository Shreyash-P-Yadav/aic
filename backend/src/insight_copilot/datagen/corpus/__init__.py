"""L6 — the unstructured corpus, generated FROM the event ledger.

Every document is causally consistent with the numbers, because it is generated from
the event that caused them. Critically, the *absence* of documents is designed too:
about 15% of events get none at all, which is what exercises the sufficiency check.
"""
